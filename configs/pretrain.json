{
  "model_config_path": "configs/model.json",
  "train_data_path": "data/processed/train.bin",
  "validation_data_path": "data/processed/validation.bin",
  "output_dir": "artifacts/checkpoints",
  "batch_size": 8,
  "gradient_accumulation_steps": 4,
  "learning_rate": 0.00025,
  "weight_decay": 0.01,
  "warmup_steps": 100,
  "max_steps": 5000,
  "max_grad_norm": 1.0,
  "log_interval": 10,
  "eval_interval": 100,
  "save_interval": 500,
  "num_workers": 0,
  "seed": 42,
  "precision": "fp32"
}
